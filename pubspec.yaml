name: hrms_clone
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter


  
  cupertino_icons: ^1.0.2
  flutter_bloc: ^8.1.2
  freezed_annotation: ^2.2.0
  flutter_lorem: ^2.0.0
  intl: ^0.18.0
  simple_animations: ^4.2.0
  supercharged: ^2.1.1
  flutter_quill: ^6.4.4
  
  

dev_dependencies:
  flutter_test:
    sdk: flutter

  
  flutter_lints: ^2.0.0
  freezed: ^2.3.2
  build_runner: ^2.3.3
  

flutter:

  
  uses-material-design: true

  
  assets:
    - assets/images/
    - assets/images/member_list/
    

  